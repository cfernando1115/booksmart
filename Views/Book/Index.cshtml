@model IEnumerable<BookSmart.Models.Book>

<h1>Books</h1>
<a asp-controller="Book" asp-action="Create">Add Book</a>

@if (Model.Count() > 0)
{
    @foreach(var book in Model)
    {
        <td>@book.Name</td>
    }
}
else
{
    <p>We're out of books!</p>
}

